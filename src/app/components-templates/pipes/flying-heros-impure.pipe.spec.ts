import { FlyingHerosImpurePipe } from './flying-heros-impure.pipe';

describe('FlyingHerosImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyingHerosImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
