import { FlyingHerosPurePipe } from './flying-heros-pure.pipe';

describe('FlyingHerosPurePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyingHerosPurePipe();
    expect(pipe).toBeTruthy();
  });
});
